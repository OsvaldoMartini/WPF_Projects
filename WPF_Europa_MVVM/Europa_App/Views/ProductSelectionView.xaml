<UserControl x:Class="Europa_App.Views.ProductSelectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:viewModels="clr-namespace:Europa_App.ViewModels"
    xmlns:foundation="clr-namespace:Europa_App.Foundation">
        
    <UserControl.DataContext>
        <viewModels:ProductSelectionModel />
    </UserControl.DataContext>

    <Grid>
        <ListBox Margin="10" Background="LightSkyBlue"
            ItemsSource="{Binding DataItems}" 
            DisplayMemberPath="ModelName"
            SelectedItem="{Binding SelectedProduct}"
            foundation:CommandBehavior.RoutedEventName="SelectionChanged"
            foundation:CommandBehavior.TheCommandToRun="{Binding Path=ListBoxCommand}">
        </ListBox>
    </Grid>
</UserControl>
